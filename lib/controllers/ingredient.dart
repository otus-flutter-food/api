import 'package:conduit/conduit.dart';

import '../model/ingredient.dart';

class MeasureUnitController extends ManagedObjectController<MeasureUnit> {
  MeasureUnitController(ManagedContext context) : super(context);
}

class IngredientController extends ManagedObjectController<Ingredient> {
  IngredientController(ManagedContext context) : super(context);
}

